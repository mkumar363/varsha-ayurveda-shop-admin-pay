# Server port
PORT=4000

# During development, you can restrict CORS like:
# CORS_ORIGIN=http://localhost:5173
# or multiple origins comma-separated
CORS_ORIGIN=http://localhost:5173

# Optional: change DB location
# DB_PATH=./data/db.json

# Admin key to enable admin endpoints (x-admin-key header)
ADMIN_KEY=change-me

# Optional: bootstrap an admin account on server start (recommended for the Admin UI).
# If a user with ADMIN_EMAIL does not exist, it will be created with role=admin.
# IMPORTANT: Change these values in production.
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin12345

# JWT settings
JWT_SECRET=dev-secret-change-me
JWT_EXPIRES_IN=7d

# Razorpay (Payment Gateway) - Standard Checkout
# Create keys in Razorpay dashboard and set them here.
RAZORPAY_KEY_ID=rzp_test_xxxxxxxxxxxx
RAZORPAY_KEY_SECRET=xxxxxxxxxxxxxxxxxxxx
